using UnityEngine;

public class SamepleCode : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Sample");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
